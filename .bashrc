#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return
alias ll='ls alh'
alias l='ls -CFlh'
alias woo='fortune'
alias la='ls -la
alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '

